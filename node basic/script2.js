var data = require("./script") // require() will accept the exported value from script.js file 
// and that value will be used here and is stored in data, so data will now hold '12'
console.log(data)